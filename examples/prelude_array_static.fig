const merge = @import("prelude.std");

fn inc(x: i32) -> i32 {
  x + 1
}

fn add(a: i32, b: i32) -> i32 {
  a + b
}

fn keep_gt_two(x: i32) -> bool {
  x > 2
}

pub fn main() -> i32 {
  fold4_i32(add, 0, map4_i32(inc, [1, 2, 3, 4]))
}
