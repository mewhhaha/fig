module demo.effects;

import capability clock: fn() -> i32 !{time};
import capability random: fn() -> i32 !{entropy};

pub fn main() -> i32 !{time, entropy} {
  let now = clock();
  let entropy = random();
  now + entropy
}
