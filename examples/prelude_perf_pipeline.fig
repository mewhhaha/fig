const merge = @import("prelude.std");

fn add(a: i32, b: i32) -> i32 {
  a + b
}

fn inc(x: i32) -> i32 {
  x + 1
}

pub fn main() -> i32 {
  let xs: merge.array.layout.lane4_i32 = <1, 2, 3, 4>;
  merge.array.fold4_i32(add, 2 + 4 + 4, merge.array.map4_i32(inc, xs))
}
