const operators = @import("prelude.operators");

const effect = @import("prelude.effect");

type fn Env() -> type {
  i32
}

fn add_env() -> effect.Eff({reader: Env}, i32) {
  \ctx -> ctx.reader + 2
}

pub fn main() -> i32 {
  add_env()
  \program -> effect.run_reader(program, 40)
}
