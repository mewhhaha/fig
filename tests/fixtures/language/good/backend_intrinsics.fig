fn branch.handle(ptr: i32) -> i64 { @branch_handle(ptr) }
fn branch.handle_ptr(handle: i64) -> i32 { @branch_handle_ptr(handle) }

pub fn main(ptr: i32) -> i32 {
  let handle = branch.handle(ptr);
  branch.handle_ptr(handle)
}
