// expect: parse.syntax
type fn BadLayout() -> type {
  let Layout = @wgsl_bindings(```wgsl
@group(0) @binding(1) var<uniform> camera: mat4x4<f32>;
```);
  @require(Layout == i32, "shader Layout is not i32");
  i32
}

fn use_bad(value: BadLayout()) -> i32 { value }
